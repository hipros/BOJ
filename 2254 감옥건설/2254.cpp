#include<cstdio>
#define MAXN 1000

struct Point{
	int x, y;
	Point(int tx = 0, int ty = 0) :x(tx), y(ty) {}
};
int main ()
{
	int n;
	Point pr;
	Point col[MAXN+1];
	Point o;
	scanf("%d", &n);
	scanf("%d %d", &pr.x, &pr.y);
	for(int i = 0;i < n; i++){
		scanf("%d %d", &col[i].x, &col[i].y);
		if()
	}

	return 0;
}